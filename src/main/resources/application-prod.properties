# Production Configuration
spring.application.name=WatchList-App

# Server Configuration
server.port=${PORT:8082}

# H2 Database Configuration
spring.h2.console.enabled=false
spring.datasource.url=jdbc:h2:file:./data/moviedb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.show-sql=false

# Logging Configuration
logging.level.root=INFO
logging.level.com.example.muskan=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n
logging.file.name=logs/watchlist-app.log
logging.file.max-size=10MB
logging.file.max-history=10

# Actuator for Health Checks (Optional - requires spring-boot-starter-actuator)
# management.endpoints.web.exposure.include=health,info
# management.endpoint.health.show-details=when-authorized
